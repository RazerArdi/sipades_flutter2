import 'package:flutter/material.dart';

class SuratKematianScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Surat Keterangan Kematian'),
      ),
      body: const Center(
        child: Text('Halaman Surat Keterangan Kematian'),
      ),
    );
  }
}
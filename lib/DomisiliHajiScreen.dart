import 'package:flutter/material.dart';

class DomisiliHajiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Surat Keterangan Domisili Haji'),
      ),
      body: const Center(
        child: Text('Halaman Surat Keterangan Domisili Haji'),
      ),
    );
  }
}
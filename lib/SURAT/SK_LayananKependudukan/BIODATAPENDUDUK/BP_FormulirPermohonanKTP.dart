import 'package:flutter/material.dart';

class BP_FormulirPermohonanKTP extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Formulir Permohonan KTP'),
        backgroundColor: Colors.black,
        centerTitle: true,
      ),
      body: Center(
        child: Text('Detail for Formulir Permohonan KTP (F-1.21)'),
      ),
    );
  }
}

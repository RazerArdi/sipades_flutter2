import 'package:flutter/material.dart';

class LayananUmumSkBepergian extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Surat Keterangan Bepergian'),
        backgroundColor: Colors.lightGreen,
      ),
      body: const Center(
        child: Text('Halaman Surat Keterangan Bepergian'),
      ),
    );
  }
}

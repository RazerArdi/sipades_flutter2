import 'package:flutter/material.dart';

class LayananUmumSkTidakMampuSekolah extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Surat Keterangan Tidak Mampu (Sekolah)'),
        backgroundColor: Colors.purple,
      ),
      body: const Center(
        child: Text('Halaman Surat Keterangan Tidak Mampu (Sekolah)'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class LayananUmumSkBedaIdentitas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Surat Keterangan Beda Identitas'),
        backgroundColor: Colors.lime,
      ),
      body: const Center(
        child: Text('Halaman Surat Keterangan Beda Identitas'),
      ),
    );
  }
}

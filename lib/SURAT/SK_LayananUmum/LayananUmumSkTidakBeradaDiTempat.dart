import 'package:flutter/material.dart';

class LayananUmumSkTidakBeradaDiTempat extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Surat Keterangan Tidak Berada di Tempat'),
        backgroundColor: Colors.amber,
      ),
      body: const Center(
        child: Text('Halaman Surat Keterangan Tidak Berada di Tempat'),
      ),
    );
  }
}

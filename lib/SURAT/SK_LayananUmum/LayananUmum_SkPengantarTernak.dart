import 'package:flutter/material.dart';

class LayananUmumSkPengantarTernak extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Surat Keterangan Pengantar Ternak'),
        backgroundColor: Colors.orange,
      ),
      body: const Center(
        child: Text('Halaman Surat Keterangan Pengantar Ternak'),
      ),
    );
  }
}

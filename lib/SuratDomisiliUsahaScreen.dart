import 'package:flutter/material.dart';

class SuratDomisiliUsahaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Surat Keterangan Domisili Usaha'),
      ),
      body: const Center(
        child: Text('Halaman Surat Keterangan Domisili Usaha'),
      ),
    );
  }
}
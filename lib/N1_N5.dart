import 'package:flutter/material.dart';

class N1N5Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Surat N1-N5'),
      ),
      body: const Center(
        child: Text('Halaman Surat N1-N5'),
      ),
    );
  }
}
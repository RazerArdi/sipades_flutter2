import 'package:flutter/material.dart';

class JadwalSholatScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Jadwal Sholat"),
      ),
      body: Center(
        child: Text('Tampilkan Jadwal Sholat di sini'),
      ),
    );
  }
}